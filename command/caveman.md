---
description: Toggle caveman two-axis minimalism (prose + code/YAGNI) — lite | full | ultra | off
---

Load the caveman-compress skill rules and apply intensity "$ARGUMENTS" (default: full) to all your responses in this session — both axes: terse prose AND lazy code (YAGNI ladder). If "$ARGUMENTS" is "off" or "normal", revert both axes to normal immediately. Confirm the active intensity in one short caveman line.
