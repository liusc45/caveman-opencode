---
description: Toggle caveman compressed-speech mode (lite | full | ultra | off)
---

Load the caveman-compress skill rules and apply intensity "$ARGUMENTS" (default: full) to all your responses in this session. If "$ARGUMENTS" is "off" or "normal", revert to normal prose immediately. Confirm the active intensity in one short caveman line.
